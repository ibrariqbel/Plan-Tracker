@{
    ViewData["Title"] = "Login";
}

<!-- Background Wrapper -->
<div class="container-fluid d-flex justify-content-center align-items-center vh-100 bg-neumorphism">
    <div class="neumorphic-card p-4">
        <h2 class="text-center mb-3">Login to Your Account</h2>

        <form method="post" asp-controller="User" asp-action="Login">
            <div class="mb-3">
                <label for="exampleInputEmail1" class="form-label">Email address</label>
                <input type="email" class="form-control input-neumorphic" id="exampleInputEmail1" name="Email" required>
                <div class="form-text">We'll never share your email.</div>
            </div>

            <div class="mb-3">
                <label for="exampleInputPassword1" class="form-label">Password</label>
                <input type="password" class="form-control input-neumorphic" id="exampleInputPassword1" name="Password" required>
            </div>

            <div class="mb-3 form-check">
                <input type="checkbox" class="form-check-input" id="exampleCheck1">
                <label class="form-check-label" for="exampleCheck1">Remember Me</label>
            </div>

            <button type="submit" class="btn-neumorphic w-100">Login</button>
            <div class="text-center mt-3">
                <a asp-controller="User" asp-action="Register" class="btn btn-link">Register Now</a>
            </div>

            <p class="text-danger text-center pt-2"> @ViewBag.errorMessage </p>
        </form>
    </div>
</div>

<!-- Neumorphism CSS -->
<style>
    /* Background */
    .bg-neumorphism {
        background: #e0e0e0;
        height: 100vh;
    }

    /* Card Design */
    .neumorphic-card {
        width: 400px;
        padding: 20px;
        border-radius: 15px;
        background: #e0e0e0;
        box-shadow: 8px 8px 15px #c8c8c8, -8px -8px 15px #ffffff;
    }

    /* Input Fields */
    .input-neumorphic {
        background: #e0e0e0;
        border: none;
        padding: 10px;
        border-radius: 8px;
        box-shadow: inset 4px 4px 10px #c8c8c8, inset -4px -4px 10px #ffffff;
    }

    /* Buttons */
    .btn-neumorphic {
        background: #e0e0e0;
        border: none;
        padding: 12px;
        border-radius: 8px;
        box-shadow: 4px 4px 10px #c8c8c8, -4px -4px 10px #ffffff;
        font-weight: bold;
        transition: 0.3s;
    }

    .btn-neumorphic:hover {
        box-shadow: inset 4px 4px 10px #c8c8c8, inset -4px -4px 10px #ffffff;
    }
</style>
